#include"God.hpp"


int God::GodId = 0;

// template <typename eventType>
// bool God::postEvent(const eventType &eventData)
// {
//     Event<eventType> event(GodId, eventData);

//     // Assuming EldenTree is a singleton or globally accessible
//     // EldenTree::getInstance().postEvent(Name, event);
//     return true;
// }